/**
 * VIET CITY WEAR — Supabase Client Entry Point
 * Re-exporting client singleton & factory for easy imports:
 * `import { supabase } from "@/lib/supabase";`
 */
export { supabase, createClient } from "./supabase/client";
